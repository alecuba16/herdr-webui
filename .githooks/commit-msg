#!/usr/bin/env bash
set -euo pipefail

subject="$(sed -n '1p' "$1")"

if ! printf '%s\n' "$subject" | grep -Eq '^(build|chore|ci|docs|feat|fix|perf|refactor|revert|style|test)(\([[:alnum:]_.-]+\))?!?: .+'; then
  echo "commit-msg: expected Conventional Commit subject" >&2
  exit 1
fi
